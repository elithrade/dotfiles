- defaults:
    link:
      create: true
      relink: true

- clean: ['~', ~/.config]

- link:
    ~/.wallpapers: wallpapers

- shell:
  - [git submodule update --init --recursive, Installing submodules]
